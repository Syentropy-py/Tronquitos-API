{
  "buildCommand": "echo 'Frontend build'",
  "outputDirectory": "frontend",
  "installCommand": "echo 'No install needed'",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
